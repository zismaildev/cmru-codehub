# 🔐 MD5 Secure Auth System (คำแนะนำในการติดตั้งและใช้งาน)

> [!NOTE]
> โครงสร้างโปรเจคได้รับการปรับปรุงเพื่อความเป็นระเบียบมากขึ้น

## โครงสร้าง Folder

```
MD5-Secure-Auth-System/
├── assets/              # Static files
│   ├── css/
│   │   └── style.css
│   ├── js/              # สำหรับ JavaScript (ถ้ามี)
│   └── images/          # สำหรับรูปภาพ (ถ้ามี)
├── config/              # Configuration files
│   └── database.php
├── database/            # Database scripts
│   └── setup.sql
├── pages/               # หน้าเว็บหลัก
│   ├── auth.php         # ตรวจสอบ login
│   ├── login.php        # หน้า login form
│   ├── logout.php       # ออกจากระบบ
│   └── welcome.php      # หน้าต้อนรับ
├── index.php            # หน้าแรก (auto redirect)
└── README.md
```

## ขั้นตอนที่ 1: ตั้งค่าฐานข้อมูล

1. เปิด XAMPP Control Panel
2. เริ่มต้น Apache และ MySQL
3. เปิดเบราว์เซอร์ไปที่ `http://localhost/phpmyadmin`
4. คลิก **Import** หรือเปิด SQL tab
5. Copy คำสั่ง SQL จากไฟล์ [database/setup.sql](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/database/setup.sql)
6. Paste และ Execute คำสั่ง

หรือใช้คำสั่งนี้ใน Terminal:
```bash
mysql -u root -p < c:\xampp\htdocs\MD5-Secure-Auth-System\database\setup.sql
```

## ขั้นตอนที่ 2: ตรวจสอบไฟล์ที่สร้าง

ระบบประกอบด้วยไฟล์ดังนี้:

### ฐานข้อมูล
- [database/setup.sql](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/database/setup.sql) - SQL script สำหรับสร้างฐานข้อมูลและตาราง

### Backend PHP
- [config/database.php](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/config/database.php) - ไฟล์เชื่อมต่อฐานข้อมูล
- [pages/auth.php](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/pages/auth.php) - ตรวจสอบการ login (MD5)
- [pages/logout.php](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/pages/logout.php) - ออกจากระบบ

### Frontend
- [index.php](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/index.php) - หน้าแรก (auto redirect)
- [pages/login.php](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/pages/login.php) - หน้า Login Form
- [pages/welcome.php](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/pages/welcome.php) - หน้าต้อนรับ
- [assets/css/style.css](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/assets/css/style.css) - Custom CSS

## ขั้นตอนที่ 3: ทดสอบระบบ

1. เปิดเบราว์เซอร์ไปที่: `http://localhost/MD5-Secure-Auth-System/` หรือ `http://localhost/MD5-Secure-Auth-System/pages/login.php`

2. **ทดสอบกรณีที่ 1: Login สำเร็จ**
   - Username: `admin`
   - Password: `admin123`
   - ผลลัพธ์: ควรแสดงหน้า `pages/welcome.php` พร้อมข้อความ "Welcome to Mini Project System Website"

3. **ทดสอบกรณีที่ 2: Login สำเร็จ (บัญชีที่ 2)**
   - Username: `student`
   - Password: `student123`
   - ผลลัพธ์: ควรแสดงหน้า `pages/welcome.php`

4. **ทดสอบกรณีที่ 3: Login ไม่สำเร็จ**
   - Username: `wrong`
   - Password: `wrong123`
   - ผลลัพธ์: ควรแสดงข้อความ "CAN NOT LOGIN" ในหน้า `pages/login.php`

5. **ทดสอบ Logout**
   - คลิกปุ่ม "ออกจากระบบ"
   - ผลลัพธ์: ควรกลับไปหน้า `pages/login.php` พร้อมข้อความ "ออกจากระบบเรียบร้อยแล้ว"

## คุณสมบัติของระบบ

✅ ใช้ **PHP** สำหรับ Backend Logic  
✅ ใช้ **Bootstrap 5** สำหรับ UI/UX ที่สวยงาม  
✅ ใช้ **MySQL** สำหรับจัดเก็บข้อมูลผู้ใช้  
✅ ใช้ **MD5** สำหรับเข้ารหัสรหัสผ่าน  
✅ มี **Session Management** สำหรับการจัดการ login  
✅ มี **SQL Injection Protection** ด้วย Prepared Statements  
✅ แสดงข้อความ **"CAN NOT LOGIN"** เมื่อ login ไม่สำเร็จ  
✅ แสดงข้อความ **"Welcome to ... Website"** เมื่อ login สำเร็จ

## การปรับแต่ง

### เปลี่ยนข้อความต้อนรับ

แก้ไขไฟล์ [pages/welcome.php](file:///c:/xampp/htdocs/MD5-Secure-Auth-System/pages/welcome.php) บรรทัดที่ 19:

```php
$student_info = "ชื่อ ชุด วิชา หมู่เรียน ของคุณ";
```

### เพิ่มผู้ใช้ใหม่

เข้า phpMyAdmin และเพิ่มข้อมูลในตาราง users:

```sql
INSERT INTO users (username, password, full_name) VALUES
('username_ใหม่', MD5('password_ใหม่'), 'ชื่อเต็ม');
```

### เปลี่ยนรหัสผ่าน

```sql
UPDATE users 
SET password = MD5('รหัสผ่านใหม่')
WHERE username = 'admin';
```

## หมายเหตุด้านความปลอดภัย

> [!WARNING]
> **การใช้ MD5 ไม่แนะนำสำหรับการใช้งานจริง** เนื่องจากไม่ปลอดภัยเพียงพอ  
> สำหรับการใช้งานจริง ควรใช้ `password_hash()` และ `password_verify()` แทน

> [!TIP]
> หากต้องการความปลอดภัยเพิ่มเติม ควรเพิ่ม:
> - HTTPS
> - CSRF Protection
> - Rate Limiting
> - Password Hashing (bcrypt, argon2)
