# 💱 Currency Exchange Quiz

![Flutter](https://img.shields.io/badge/Flutter-02569B?style=flat-square&logo=flutter&logoColor=white)
![Dart](https://img.shields.io/badge/Dart-0175C2?style=flat-square&logo=dart&logoColor=white)

## 📋 คำอธิบาย (Description)
แอปพลิเคชันแปลงสกุลเงิน (Currency Exchange) ที่พัฒนาด้วย Flutter สำหรับแบบทดสอบความรู้ในการสร้าง UI และจัดการ State

โปรแกรมนี้ใช้สำหรับการแปลงเงินบาทไทย (THB) ไปเป็นสกุลเงินต่างประเทศ ได้แก่:
- 💵 **Dollar (USD)** - เงินดอลลาร์สหรัฐ
- 💴 **Chinese Yuan (CNY)** - เงินหยวนจีน
- 💴 **Japanese Yen (JPY)** - เงินเยนญี่ปุ่น
- 💴 **Korean Won (KRW)** - เงินวอนเกาหลี

## 🎯 Key Learnings (สิ่งที่เรียนรู้)

### 1. State Management (StatefulWidget)
- การใช้ `StatefulWidget` เพื่อจัดการ State ของ UI ที่เปลี่ยนแปลงได้
- การใช้ `setState()` เพื่ออัปเดต UI เมื่อมีการเปลี่ยนแปลงข้อมูล

### 2. User Input (TextFormField)
- การรับ Input จากผู้ใช้ด้วย `TextFormField`
- การใช้ `TextEditingController` เพื่อดึงค่าจาก TextField

### 3. Radio Button Selection
- การใช้ `RadioListTile` เพื่อให้ผู้ใช้เลือกตัวเลือกเดียวจากหลายตัวเลือก
- การจัดการ `groupValue` เพื่อควบคุมการเลือก Radio Button

### 4. Dialog (Alert Dialog)
- การแสดงผลลัพธ์ด้วย `AlertDialog`
- การใช้ `showDialog()` เพื่อแสดง Popup

### 5. String Interpolation & Formatting
- การใช้ `${variable}` สำหรับแทรกค่าตัวแปรใน String
- การใช้ `.toStringAsFixed(2)` เพื่อจำกัดทศนิยม 2 ตำแหน่ง

## 🧮 Exchange Rates (อัตราแลกเปลี่ยน)
โปรแกรมใช้อัตราแลกเปลี่ยนคงที่ (Fixed Rate) ดังนี้:

| สกุลเงิน | อัตราต่อ 1 บาท |
|:---|---:|
| THB (บาท) | 1.00 |
| USD (ดอลลาร์) | 0.031 |
| CNY (หยวน) | 0.022 |
| JPY (เยน) | 5.00 |
| KRW (วอน) | 46.00 |

> **หมายเหตุ:** อัตราแลกเปลี่ยนที่ใช้ในโปรแกรมเป็นค่าประมาณการเพื่อการเรียนรู้เท่านั้น ไม่ใช่อัตราจริงในตลาด

## 🎨 UI Components
- **AppBar**: หัวข้อแอปพลิเคชัน (สีม่วง Deep Purple)
- **TextFormField**: ช่องกรอกจำนวนเงิน
- **RadioListTile** (x4): ปุ่มเลือกสกุลเงินปลายทาง
- **ElevatedButton**: ปุ่มกดเพื่อคำนวณ
- **AlertDialog**: แสดงผลลัพธ์การแปลงเงิน

## 🚀 วิธีการรัน (How to Run)
1. ติดตั้ง Flutter SDK
2. เปิดโปรเจกต์ใน VS Code หรือ Android Studio
3. รันคำสั่ง:
   ```bash
   flutter run main.dart
   ```
4. หรือกด `F5` ใน VS Code

## 📸 Usage (ตัวอย่างการใช้งาน)
1. กรอกจำนวนเงินบาท (เช่น 1000)
2. เลือกสกุลเงินที่ต้องการแปลง (เช่น Dollar)
3. กดปุ่ม **Convert**
4. ดูผลลัพธ์ใน Dialog ที่แสดงขึ้นมา

**ตัวอย่าง:**
- Input: `1000 THB`
- เลือก: `Dollar`
- Output: `31.00 Dollar`

## 🛠️ Technical Stack
- **Language:** Dart
- **Framework:** Flutter SDK
- **State Management:** StatefulWidget + setState()
- **UI Components:** Material Design

## 📝 Notes
- โค้ดนี้เป็นส่วนหนึ่งของการทำ Quiz ในรายวิชา Mobile Application Development
- เน้นการฝึกพื้นฐาน Flutter UI และ State Management
- ยังไม่มีการจัดการ Error (เช่น Input ไม่ใช่ตัวเลข)
