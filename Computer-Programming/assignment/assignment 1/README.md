# 🐍 Python Assignment 1: Syntax & I/O

<div align="center">

![Topic](https://img.shields.io/badge/Topic-Input_Output-3776AB?style=for-the-badge)
![Level](https://img.shields.io/badge/Level-Beginner-green?style=for-the-badge)

**"Your First Steps in Python Programming"**

</div>

---

## 🎯 Learning Objectives

หลังจากทำ Assignment นี้เสร็จ คุณจะสามารถ:
- ✅ เขียนโปรแกรม Python พื้นฐานได้
- ✅ ใช้ `print()` แสดงผลข้อมูล
- ✅ ใช้ตัวแปรเก็บค่าและคำนวณ
- ✅ เข้าใจ Data Types (int, float, str)
- ✅ แปลง Type ด้วย `int()`, `float()`, `str()`
- ✅ ตรวจสอบชื่อตัวแปรที่ถูกต้อง

---

## 📝 Problem Sets

### ข้อ 1: แสดงข้อมูลนักศึกษา
**โจทย์:** แสดงรหัสนักศึกษา, ชื่อ-นามสกุล, และจังหวัดที่อยู่อาศัย

**ตัวอย่างผลลัพธ์:**
```
66143420
ณัฐพงษ์ ปันธิยะ
เชียงใหม่
```

**Concept:** การใช้ `print()` เพื่อแสดงผลข้อความ

---

### ข้อ 2: แสดงอายุ
**โจทย์:** แสดงอายุของคุณ

**ตัวอย่างผลลัพธ์:**
```
19
```

**Concept:** การแสดงผลตัวเลข (Integer)

---

### ข้อ 3: คำนวณพื้นที่สามเหลี่ยม
**โจทย์:** เขียนโปรแกรมหาพื้นที่สามเหลี่ยม เมื่อ **ฐาน = 10** และ **สูง = 20**

**สูตร:** `พื้นที่ = 0.5 × ฐาน × สูง`

**ตัวอย่างผลลัพธ์:**
```
พื้นที่ของสามเหลี่ยม :  100.0
```

**Code Solution:**
```python
base = 10
height = 20
area = 0.5 * base * height
print("พื้นที่ของสามเหลี่ยม : ", area)
```

**Concept:** 
- การใช้ตัวแปรเก็บค่า
- การคำนวณทางคณิตศาสตร์
- Operator `*` (คูณ)

---

### ข้อ 4: คำนวณพื้นที่สี่เหลี่ยมผืนผ้า
**โจทย์:** เขียนโปรแกรมหาพื้นที่สี่เหลี่ยมผืนผ้า เมื่อ **กว้าง = 100** และ **ยาว = 400**

**สูตร:** `พื้นที่ = กว้าง × ยาว`

**ตัวอย่างผลลัพธ์:**
```
พื้นที่สี่เหลี่ยมผืนผ้า :  40000
```

**Code Solution:**
```python
width = 100
height = 400
area = width * height
print("พื้นที่สี่เหลี่ยมผืนผ้า : ", area)
```

---

### ข้อ 5: ตรวจสอบชื่อตัวแปร
**โจทย์:** ข้อใดต่อไปนี้เป็นชื่อตัวแปรที่ถูกต้องใน Python?

1. `Gpa` → ✅ ถูกต้อง
2. `_ProgramPython` → ✅ ถูกต้อง (ขึ้นต้นด้วย `_` ได้)
3. `Num` → ✅ ถูกต้อง
4. `email@cmru` → ❌ **ผิด** เพราะมีอักษรพิเศษ `@`
5. `64122456ID` → ❌ **ผิด** เพราะขึ้นต้นด้วยตัวเลข

**กฎการตั้งชื่อตัวแปรใน Python:**
- ✅ ขึ้นต้นด้วยตัวอักษร (a-z, A-Z) หรือ underscore (_)
- ✅ ตามด้วยตัวอักษร, ตัวเลข, หรือ underscore
- ❌ ห้ามขึ้นต้นด้วยตัวเลข
- ❌ ห้ามใช้อักษรพิเศษ (@, #, $, %, etc.)
- ❌ ห้ามใช้คำสงวน (if, for, while, def, etc.)

---

### ข้อ 6: Type Conversion - สามเหลี่ยม
**โจทย์:** คำนวณพื้นที่สามเหลี่ยม แสดงชนิดตัวแปร และแปลงเป็น `int`

**Code Solution:**
```python
b = 10
h = 20
area = 0.5 * b * h
print("พื้นที่สามเหลี่ยมเท่ากับ : ", area)
print(type(area))   # แสดงชนิดตัวแปร
print(int(area))    # แปลงเป็น int
```

**ผลลัพธ์:**
```
พื้นที่สามเหลี่ยมเท่ากับ :  100.0
<class 'float'>
100
```

**Concept:**
- `type()` - ตรวจสอบชนิดข้อมูล
- `int()` - แปลงเป็นจำนวนเต็ม (ตัดทศนิยมทิ้ง)
- `100.0` → `100`

---

### ข้อ 7: Type Conversion - สี่เหลี่ยม
**โจทย์:** คำนวณพื้นที่สี่เหลี่ยมผืนผ้า แสดงชนิดตัวแปร และแปลงเป็น `str`

**Code Solution:**
```python
wide = 100
longs = 400
area = wide * longs
print("พื้นที่สี่เหลี่ยมเท่ากับ : ", area)
print(type(area))   # แสดงชนิดตัวแปร
print(str(area))    # แปลงเป็น str
```

**ผลลัพธ์:**
```
พื้นที่สี่เหลี่ยมเท่ากับ :  40000
<class 'int'>
40000
```

**Concept:**
- `str()` - แปลงเป็น String (ข้อความ)
- เมื่อแปลงเป็น String แล้ว จะไม่สามารถนำไปคำนวณต่อได้

---

## 💡 Key Takeaways

### 1. การใช้ print()
```python
print("Hello")        # แสดงข้อความ
print(123)            # แสดงตัวเลข
print("ผลลัพธ์:", 100) # แสดงหลายค่า
```

### 2. การประกาศตัวแปร
```python
x = 10        # Integer
y = 3.14      # Float
name = "John" # String
```

### 3. Data Types
| Type | คำอธิบาย | ตัวอย่าง |
|:---|:---|:---|
| `int` | จำนวนเต็ม | `10`, `-5`, `0` |
| `float` | ทศนิยม | `3.14`, `0.5`, `-2.0` |
| `str` | ข้อความ | `"Hello"`, `'Python'` |

### 4. Type Conversion
```python
int("123")     # "123" → 123
float("3.14")  # "3.14" → 3.14
str(456)       # 456 → "456"
int(3.9)       # 3.9 → 3 (ตัดทศนิยมทิ้ง!)
```

---

## 🚀 How to Run

```bash
python assignment_1.py
```

---

## 📚 Additional Resources
- [Python Official Tutorial](https://docs.python.org/3/tutorial/)
- [Python Variables - W3Schools](https://www.w3schools.com/python/python_variables.asp)
- [Python Data Types - Real Python](https://realpython.com/python-data-types/)

